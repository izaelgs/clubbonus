import {
  Text,
  ScrollView,
} from "tamagui";

export default function Establishment() {
	return (
		<ScrollView
			padding="$4"
			bounces={false}
      backgroundColor="white">
			<Text>Establishment Dashboard</Text>
		</ScrollView>
	);
}
